---
import AppLayout from "@/layouts/AppLayout.astro";
import { CalendarViewWrapper } from "@/components/CalendarViewWrapper";

// Auth check is handled by middleware
// User will be redirected to login if not authenticated
---

<AppLayout title="Calendar - Bloom Learning">
  <CalendarViewWrapper client:only="react" />
</AppLayout>
